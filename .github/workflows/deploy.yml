name: Deploy Electrical Accounting

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      
    - name: 🔍 List files
      run: |
        echo "📁 الملفات الموجودة:"
        ls -la
        echo ""
        echo "📄 محتوى الجذر:"
        find . -maxdepth 1 -type f | sort
        
    - name: 🚀 Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./
        enable_jekyll: false
        cname: ""
        
    - name: 📢 Success Message
      run: |
        echo "🎉 تم النشر بنجاح!"
        echo "🌐 الرابط: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"
        echo "⏰ الوقت: $(date)"
