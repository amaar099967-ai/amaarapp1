name: First Deploy - Electrical Accounting

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:  # للتحكم اليدوي

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 استلام الكود
      uses: actions/checkout@v4
      with:
        persist-credentials: false
        
    - name: 🏗️ إنشاء الملفات الأساسية
      run: |
        echo "📁 إنشاء هيكل التطبيق..."
        
        # 1. إنشاء index.html
        cat > index.html << 'EOF'
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#000428">
    <title>⚡ حسابات نهائية لأعمال الكهرباء</title>
    <link rel="manifest" href="manifest.json">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        :root {
            --primary: #00ff88;
            --secondary: #004e92;
            --dark: #000428;
            --light: #f0f8ff;
            --success: #00c853;
            --warning: #ff9800;
            --danger: #f44336;
        }
        
        body {
            font-family: 'Cairo', sans-serif;
            background: linear-gradient(135deg, var(--dark), var(--secondary));
            color: var(--light);
            min-height: 100vh;
            line-height: 1.6;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
        }
        
        header {
            text-align: center;
            padding: 40px 20px;
            background: rgba(0, 0, 0, 0.4);
            border-radius: 20px;
            margin-bottom: 40px;
            border: 2px solid var(--primary);
            box-shadow: 0 10px 30px rgba(0, 255, 136, 0.2);
        }
        
        .logo {
            font-size: 3.5rem;
            color: var(--primary);
            margin-bottom: 20px;
            animation: pulse 2s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        h1 {
            font-size: 2.8rem;
            margin-bottom: 10px;
            background: linear-gradient(90deg, var(--primary), #00ffcc);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 0 20px rgba(0, 255, 136, 0.3);
        }
        
        .subtitle {
            font-size: 1.4rem;
            color: #88ffcc;
            margin-bottom: 30px;
        }
        
        .stats {
            display: flex;
            justify-content: center;
            gap: 30px;
            flex-wrap: wrap;
            margin: 30px 0;
        }
        
        .stat-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 25px;
            min-width: 200px;
            text-align: center;
            border: 1px solid rgba(0, 255, 136, 0.3);
            transition: transform 0.3s;
        }
        
        .stat-card:hover {
            transform: translateY(-10px);
            border-color: var(--primary);
        }
        
        .stat-card i {
            font-size: 2.5rem;
            color: var(--primary);
            margin-bottom: 15px;
        }
        
        .stat-card h3 {
            font-size: 1.1rem;
            margin-bottom: 10px;
            color: var(--primary);
        }
        
        .stat-card p {
            font-size: 2rem;
            font-weight: bold;
            color: white;
        }
        
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            margin: 40px 0;
        }
        
        .feature-card {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 15px;
            padding: 25px;
            border-left: 5px solid var(--primary);
            transition: all 0.3s;
        }
        
        .feature-card:hover {
            background: rgba(0, 255, 136, 0.1);
            transform: translateX(10px);
        }
        
        .feature-card h3 {
            color: var(--primary);
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .feature-card ul {
            list-style: none;
            padding-right: 20px;
        }
        
        .feature-card li {
            margin-bottom: 10px;
            padding-right: 10px;
            position: relative;
        }
        
        .feature-card li:before {
            content: "✓";
            color: var(--primary);
            position: absolute;
            right: -20px;
        }
        
        .setup-steps {
            background: rgba(0, 0, 0, 0.5);
            border-radius: 15px;
            padding: 30px;
            margin: 40px 0;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }
        
        .step {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 25px;
            padding: 20px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 10px;
            transition: background 0.3s;
        }
        
        .step:hover {
            background: rgba(0, 255, 136, 0.1);
        }
        
        .step-number {
            background: var(--primary);
            color: var(--dark);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            font-weight: bold;
            flex-shrink: 0;
        }
        
        .deploy-status {
            background: rgba(0, 100, 255, 0.2);
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            margin-top: 40px;
            border: 2px solid rgba(0, 100, 255, 0.4);
        }
        
        .status-badge {
            display: inline-block;
            background: var(--success);
            color: white;
            padding: 10px 25px;
            border-radius: 50px;
            font-weight: bold;
            margin-bottom: 20px;
            animation: blink 1.5s infinite;
        }
        
        @keyframes blink {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }
        
        footer {
            text-align: center;
            margin-top: 50px;
            padding-top: 30px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: #88aadd;
        }
        
        @media (max-width: 768px) {
            h1 { font-size: 2rem; }
            .subtitle { font-size: 1.1rem; }
            .stat-card { min-width: 150px; }
            .step { flex-direction: column; text-align: center; }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="logo">
                <i class="fas fa-bolt"></i>
            </div>
            <h1>حسابات نهائية لأعمال الكهرباء</h1>
            <p class="subtitle">برنامج محاسبي متكامل للمقاولات الكهربائية - الإصدار 1.0</p>
            
            <div class="stats">
                <div class="stat-card">
                    <i class="fas fa-calculator"></i>
                    <h3>المعاملات</h3>
                    <p id="transactions-count">0</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-file-invoice-dollar"></i>
                    <h3>الفواتير</h3>
                    <p id="invoices-count">0</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-chart-line"></i>
                    <h3>التقارير</h3>
                    <p id="reports-count">0</p>
                </div>
                <div class="stat-card">
                    <i class="fas fa-users"></i>
                    <h3>العملاء</h3>
                    <p id="customers-count">0</p>
                </div>
            </div>
        </header>
        
        <section class="features">
            <div class="feature-card">
                <h3><i class="fas fa-wallet"></i> إدارة مالية متكاملة</h3>
                <ul>
                    <li>تسجيل الإيرادات والمصروفات</li>
                    <li>متابعة الذمم المدينة والدائنة</li>
                    <li>تحديد الميزانيات التقديرية</li>
                    <li>تحليل التكاليف والأرباح</li>
                </ul>
            </div>
            
            <div class="feature-card">
                <h3><i class="fas fa-file-invoice"></i> نظام الفواتير</h3>
                <ul>
                    <li>إنشاء فواتير احترافية</li>
                    <li>تخصيص قوالب الفواتير</li>
                    <li>إشعارات الدفع</li>
                    <li>تقارير المبيعات</li>
                </ul>
            </div>
            
            <div class="feature-card">
                <h3><i class="fas fa-chart-pie"></i> لوحة التحكم</h3>
                <ul>
                    <li>إحصائيات فورية</li>
                    <li>رسوم بيانية تفاعلية</li>
                    <li>تنبيهات مالية</li>
                    <li>مؤشرات الأداء</li>
                </ul>
            </div>
        </section>
        
        <section class="setup-steps">
            <h2 style="text-align: center; color: var(--primary); margin-bottom: 30px;">
                <i class="fas fa-rocket"></i> خطوات البدء السريعة
            </h2>
            
            <div class="step">
                <div class="step-number">1</div>
                <div>
                    <h3 style="color: white;">إضافة العملاء</h3>
                    <p>قم بإضافة بيانات عملائك وطبيعة أعمالهم</p>
                </div>
            </div>
            
            <div class="step">
                <div class="step-number">2</div>
                <div>
                    <h3 style="color: white;">تسجيل المشاريع</h3>
                    <p>سجل تفاصيل كل مشروع كهربائي</p>
                </div>
            </div>
            
            <div class="step">
                <div class="step-number">3</div>
                <div>
                    <h3 style="color: white;">إدخال المعاملات</h3>
                    <p>ابدأ بتسجيل المعاملات المالية اليومية</p>
                </div>
            </div>
            
            <div class="step">
                <div class="step-number">4</div>
                <div>
                    <h3 style="color: white;">إصدار التقارير</h3>
                    <p>استخرج التقارير المالية الشهرية</p>
                </div>
            </div>
        </section>
        
        <div class="deploy-status">
            <div class="status-badge">
                <i class="fas fa-check-circle"></i> تم النشر بنجاح
            </div>
            <h3>✨ النظام جاهز للاستخدام ✨</h3>
            <p style="margin: 15px 0;">
                تم نشر تطبيق الحسابات الكهربائية بنجاح باستخدام GitHub Actions
            </p>
            <p style="color: #88ffcc;">
                <i class="fas fa-clock"></i> آخر تحديث: 
                <span id="current-date-time"></span>
            </p>
            <p style="margin-top: 20px; font-size: 0.9rem;">
                <i class="fas fa-info-circle"></i> 
                يتم تحديث النظام تلقائياً مع كل تغيير
            </p>
        </div>
        
        <footer>
            <p>© 2024 نظام حسابات نهائية لأعمال الكهرباء | الإصدار 1.0.0</p>
            <p style="margin-top: 10px; font-size: 0.9rem;">
                <i class="fas fa-code"></i> تم التطوير باستخدام GitHub Actions
                | 
                <i class="fas fa-mobile-alt"></i> يدعم جميع الأجهزة
            </p>
        </footer>
    </div>
    
    <script>
        // تحديث التاريخ والوقت
        function updateDateTime() {
            const now = new Date();
            const options = {
                weekday: 'long',
                year: 'numeric',
                month: 'long',
                day: 'numeric',
                hour: '2-digit',
                minute: '2-digit',
                second: '2-digit',
                timeZone: 'Asia/Riyadh'
            };
            document.getElementById('current-date-time').textContent = 
                now.toLocaleDateString('ar-SA', options);
        }
        
        // محاكاة إحصائيات
        function updateStats() {
            document.getElementById('transactions-count').textContent = Math.floor(Math.random() * 1000);
            document.getElementById('invoices-count').textContent = Math.floor(Math.random() * 500);
            document.getElementById('reports-count').textContent = Math.floor(Math.random() * 100);
            document.getElementById('customers-count').textContent = Math.floor(Math.random() * 200);
        }
        
        // بدء التطبيق
        document.addEventListener('DOMContentLoaded', function() {
            updateDateTime();
            updateStats();
            
            // تحديث كل دقيقة
            setInterval(updateDateTime, 60000);
            setInterval(updateStats, 10000);
            
            // تسجيل Service Worker
            if ('serviceWorker' in navigator) {
                navigator.serviceWorker.register('/sw.js')
                    .then(reg => console.log('✅ Service Worker مسجل:', reg.scope))
                    .catch(err => console.log('❌ خطأ في Service Worker:', err));
            }
            
            console.log('🚀 تطبيق الحسابات الكهربائية جاهز للعمل!');
        });
        
        // تأثيرات تفاعلية
        document.querySelectorAll('.feature-card').forEach(card => {
            card.addEventListener('click', () => {
                card.style.transform = 'scale(0.98)';
                setTimeout(() => {
                    card.style.transform = '';
                }, 200);
            });
        });
    </script>
</body>
</html>
EOF

        # 2. إنشاء manifest.json
        cat > manifest.json << 'EOF'
{
  "name": "حسابات نهائية لأعمال الكهرباء",
  "short_name": "حسابات الكهرباء",
  "description": "برنامج محاسبي متكامل للمقاولات الكهربائية",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#000428",
  "theme_color": "#000428",
  "orientation": "portrait-primary",
  "icons": [
    {
      "src": "https://cdn.jsdelivr.net/npm/ionicons@5.5.2/dist/svg/calculator-outline.svg",
      "sizes": "192x192",
      "type": "image/svg+xml",
      "purpose": "any maskable"
    },
    {
      "src": "https://cdn.jsdelivr.net/npm/ionicons@5.5.2/dist/svg/flash-outline.svg",
      "sizes": "512x512",
      "type": "image/svg+xml",
      "purpose": "any maskable"
    }
  ],
  "screenshots": [
    {
      "src": "https://via.placeholder.com/540x720/000428/00ff88?text=حسابات+الكهرباء",
      "sizes": "540x720",
      "type": "image/png",
      "form_factor": "narrow"
    }
  ],
  "categories": ["finance", "business", "productivity"],
  "lang": "ar",
  "dir": "rtl",
  "shortcuts": [
    {
      "name": "إضافة معاملة جديدة",
      "short_name": "معاملة",
      "description": "إضافة معاملة مالية جديدة",
      "url": "/?new-transaction",
      "icons": [{ "src": "https://cdn.jsdelivr.net/npm/ionicons@5.5.2/dist/svg/add-circle-outline.svg", "sizes": "96x96" }]
    }
  ]
}
EOF

        # 3. إنشاء service worker
        cat > sw.js << 'EOF'
// Service Worker لتطبيق الحسابات الكهربائية
const CACHE_NAME = 'electrical-accounting-v1.0';
const urlsToCache = [
  '/',
  '/index.html',
  '/manifest.json'
];

// تركيب Service Worker
self.addEventListener('install', event => {
  console.log('🔧 تثبيت Service Worker');
  event.waitUntil(
    caches.open(CACHE_NAME)
      .then(cache => {
        console.log('📦 تخزين الملفات الأساسية في الذاكرة المؤقتة');
        return cache.addAll(urlsToCache);
      })
      .then(() => self.skipWaiting())
  );
});

// تفعيل Service Worker
self.addEventListener('activate', event => {
  console.log('🚀 تفعيل Service Worker');
  event.waitUntil(
    caches.keys().then(cacheNames => {
      return Promise.all(
        cacheNames.map(cacheName => {
          if (cacheName !== CACHE_NAME) {
            console.log('🗑️ حذف الذاكرة المؤقتة القديمة:', cacheName);
            return caches.delete(cacheName);
          }
        })
      );
    }).then(() => self.clients.claim())
  );
});

// التعامل مع الطلبات
self.addEventListener('fetch', event => {
  event.respondWith(
    caches.match(event.request)
      .then(response => {
        if (response) {
          console.log('✅ استرجاع من الذاكرة المؤقتة:', event.request.url);
          return response;
        }
        
        console.log('🌐 جلب من الشبكة:', event.request.url);
        return fetch(event.request)
          .then(response => {
            if (!response || response.status !== 200 || response.type !== 'basic') {
              return response;
            }
            
            const responseToCache = response.clone();
            caches.open(CACHE_NAME)
              .then(cache => {
                cache.put(event.request, responseToCache);
              });
            
            return response;
          })
          .catch(() => {
            // صفحة البديل عند عدم الاتصال
            if (event.request.mode === 'navigate') {
              return caches.match('/');
            }
          });
      })
  );
});

// استقبال الرسائل
self.addEventListener('message', event => {
  if (event.data.action === 'skipWaiting') {
    self.skipWaiting();
  }
});
EOF

        # 4. إنشاء مجلدات CSS و JS
        mkdir -p css js images
        
        # 5. إنشاء ملف CSS أساسي
        cat > css/styles.css << 'EOF'
/* ملف الأنماط الرئيسي - تطبيق الحسابات الكهربائية */

:root {
  --primary-color: #00ff88;
  --primary-dark: #00cc6a;
  --primary-light: #88ffcc;
  --secondary-color: #004e92;
  --dark-bg: #000428;
  --card-bg: rgba(255, 255, 255, 0.1);
  --text-color: #ffffff;
  --text-secondary: #cccccc;
  --success: #00c853;
  --warning: #ff9800;
  --danger: #f44336;
  --info: #2196f3;
  --border-radius: 15px;
  --box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  --transition: all 0.3s ease;
}

/* إعادة التعيين */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Cairo', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: linear-gradient(135deg, var(--dark-bg), var(--secondary-color));
  color: var(--text-color);
  min-height: 100vh;
  line-height: 1.6;
  padding: 20px;
  overflow-x: hidden;
}

/* الطباعة */
@media print {
  body {
    background: white !important;
    color: black !important;
  }
  
  .no-print {
    display: none !important;
  }
  
  a {
    color: black !important;
    text-decoration: underline !important;
  }
}

/* الهواتف المحمولة */
@media (max-width: 768px) {
  body {
    padding: 10px;
  }
  
  h1 {
    font-size: 1.8rem !important;
  }
  
  .container {
    padding: 15px !important;
  }
  
  .features {
    grid-template-columns: 1fr !important;
  }
  
  .stats {
    flex-direction: column;
    align-items: center;
  }
  
  .stat-card {
    width: 100%;
    max-width: 300px;
  }
}

/* الأجهزة اللوحية */
@media (min-width: 769px) and (max-width: 1024px) {
  .features {
    grid-template-columns: repeat(2, 1fr) !important;
  }
}

/* تحسينات إضافية */
::selection {
  background-color: var(--primary-color);
  color: var(--dark-bg);
}

::-webkit-scrollbar {
  width: 12px;
}

::-webkit-scrollbar-track {
  background: rgba(0, 0, 0, 0.3);
  border-radius: 10px;
}

::-webkit-scrollbar-thumb {
  background: var(--primary-color);
  border-radius: 10px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--primary-dark);
}

/* تحسين الأداء */
img {
  max-width: 100%;
  height: auto;
}

/* تأثيرات التحميل */
.loading {
  display: inline-block;
  width: 50px;
  height: 50px;
  border: 3px solid rgba(255, 255, 255, 0.3);
  border-radius: 50%;
  border-top-color: var(--primary-color);
  animation: spin 1s ease-in-out infinite;
}

@keyframes spin {
  to { transform: rotate(360deg); }
}

/* تأثيرات الاهتزاز */
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  10%, 30%, 50%, 70%, 90% { transform: translateX(-5px); }
  20%, 40%, 60%, 80% { transform: translateX(5px); }
}

.shake {
  animation: shake 0.5s ease-in-out;
}

/* تلميحات */
.tooltip {
  position: relative;
  display: inline-block;
}

.tooltip .tooltiptext {
  visibility: hidden;
  background-color: var(--dark-bg);
  color: var(--text-color);
  text-align: center;
  padding: 8px 12px;
  border-radius: 8px;
  position: absolute;
  z-index: 1;
  bottom: 125%;
  left: 50%;
  transform: translateX(-50%);
  white-space: nowrap;
  opacity: 0;
  transition: opacity 0.3s;
  border: 1px solid var(--primary-color);
}

.tooltip:hover .tooltiptext {
  visibility: visible;
  opacity: 1;
}
EOF

        # 6. إنشاء ملف JavaScript أساسي
        cat > js/app.js << 'EOF
/**
 * التطبيق المحاسبي للكهرباء - الملف الرئيسي
 * الإصدار 1.0.0
 */

class ElectricalAccountingApp {
    constructor() {
        this.appName = "حسابات نهائية لأعمال الكهرباء";
        this.version = "1.0.0";
        this.storageKey = "electrical-accounting-data";
        this.isOnline = navigator.onLine;
        this.init();
    }
    
    async init() {
        console.log(`🚀 ${this.appName} - الإصدار ${this.version}`);
        
        try {
            await this.setupServiceWorker();
            this.setupEventListeners();
            this.loadInitialData();
            this.updateOnlineStatus();
            
            console.log('✅ التطبيق جاهز للاستخدام');
            this.showNotification('مرحباً بك في تطبيق الحسابات الكهربائية!', 'success');
        } catch (error) {
            console.error('❌ خطأ في تهيئة التطبيق:', error);
            this.showNotification('حدث خطأ في تحميل التطبيق', 'error');
        }
    }
    
    async setupServiceWorker() {
        if ('serviceWorker' in navigator) {
            try {
                const registration = await navigator.serviceWorker.register('/sw.js');
                console.log('✅ Service Worker مسجل:', registration.scope);
                
                // تحديث التطبيق عند توفر إصدار جديد
                registration.addEventListener('updatefound', () => {
                    const newWorker = registration.installing;
                    newWorker.addEventListener('statechange', () => {
                        if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
                            this.showUpdateNotification();
                        }
                    });
                });
            } catch (error) {
                console.warn('⚠ Service Worker فشل في التسجيل:', error);
            }
        }
    }
    
    setupEventListeners() {
        // حالة الاتصال بالإنترنت
        window.addEventListener('online', () => {
            this.isOnline = true;
            this.showNotification('تم استعادة الاتصال بالإنترنت', 'success');
        });
        
        window.addEventListener('offline', () => {
            this.isOnline = false;
            this.showNotification('فقد الاتصال بالإنترنت', 'warning');
        });
        
        // قبل إغلاق الصفحة
        window.addEventListener('beforeunload', (e) => {
            if (this.hasUnsavedChanges) {
                e.preventDefault();
                e.returnValue = 'لديك تغييرات غير محفوظة. هل تريد المغادرة؟';
            }
        });
        
        // الضغط على المفاتيح
        document.addEventListener('keydown', (e) => {
            // Ctrl + S للحفظ
            if ((e.ctrlKey || e.metaKey) && e.key === 's') {
                e.preventDefault();
                this.saveData();
            }
            
            // F1 للمساعدة
            if (e.key === 'F1') {
                e.preventDefault();
                this.showHelp();
            }
        });
    }
    
    loadInitialData() {
        try {
            const savedData = localStorage.getItem(this.storageKey);
            if (savedData) {
                const data = JSON.parse(savedData);
                console.log('📁 تم تحميل البيانات المحفوظة:', data);
                return data;
            }
            
            // بيانات أولية
            const initialData = {
                transactions: [],
                customers: [],
                projects: [],
                invoices: [],
                settings: {
                    currency: 'SAR',
                    taxRate: 15,
                    companyName: 'شركة الكهرباء',
                    lastBackup: null
                }
            };
            
            this.saveToStorage(initialData);
            return initialData;
        } catch (error) {
            console.error('❌ خطأ في تحميل البيانات:', error);
            return null;
        }
    }
    
    async saveData(data) {
        if (!this.isOnline) {
            this.showNotification('يتم العمل في وضع عدم الاتصال', 'info');
        }
        
        try {
            await this.saveToStorage(data);
            this.showNotification('تم حفظ البيانات بنجاح', 'success');
            this.hasUnsavedChanges = false;
            return true;
        } catch (error) {
            console.error('❌ خطأ في حفظ البيانات:', error);
            this.showNotification('فشل في حفظ البيانات', 'error');
            return false;
        }
    }
    
    saveToStorage(data) {
        return new Promise((resolve, reject) => {
            try {
                localStorage.setItem(this.storageKey, JSON.stringify(data));
                resolve(true);
            } catch (error) {
                reject(error);
            }
        });
    }
    
    showNotification(message, type = 'info') {
        // إنشاء عنصر الإشعار
        const notification = document.createElement('div');
        notification.className = `notification notification-${type}`;
        notification.innerHTML = `
            <i class="fas fa-${this.getIconForType(type)}"></i>
            <span>${message}</span>
            <button onclick="this.parentElement.remove()">
                <i class="fas fa-times"></i>
            </button>
        `;
        
        // إضافة الأنماط
        const style = document.createElement('style');
        style.textContent = `
            .notification {
                position: fixed;
                top: 20px;
                left: 50%;
                transform: translateX(-50%);
                background: rgba(0, 0, 0, 0.9);
                color: white;
                padding: 15px 25px;
                border-radius: 10px;
                display: flex;
                align-items: center;
                gap: 15px;
                z-index: 9999;
                border: 2px solid;
                box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
                animation: slideIn 0.3s ease-out;
            }
            
            .notification-success { border-color: #00ff88; }
            .notification-error { border-color: #ff4444; }
            .notification-warning { border-color: #ff9800; }
            .notification-info { border-color: #2196f3; }
            
            .notification button {
                background: none;
                border: none;
                color: white;
                cursor: pointer;
                font-size: 1.2rem;
                padding: 0 5px;
            }
            
            @keyframes slideIn {
                from { top: -100px; opacity: 0; }
                to { top: 20px; opacity: 1; }
            }
        `;
        
        document.head.appendChild(style);
        document.body.appendChild(notification);
        
        // إزالة الإشعار بعد 5 ثوانٍ
        setTimeout(() => {
            if (notification.parentElement) {
                notification.remove();
            }
        }, 5000);
    }
    
    getIconForType(type) {
        const icons = {
            'success': 'check-circle',
            'error': 'exclamation-circle',
            'warning': 'exclamation-triangle',
            'info': 'info-circle'
        };
        return icons[type] || 'info-circle';
    }
    
    showUpdateNotification() {
        if (confirm('يوجد تحديث جديد للتطبيق. هل تريد تحديث الصفحة الآن؟')) {
            window.location.reload();
        }
    }
    
    updateOnlineStatus() {
        const statusElement = document.getElementById('online-status');
        if (statusElement) {
            statusElement.textContent = this.isOnline ? 'متصل' : 'غير متصل';
            statusElement.style.color = this.isOnline ? '#00ff88' : '#ff4444';
        }
    }
    
    showHelp() {
        const helpContent = `
            <h2>🆘 مساعدة سريعة</h2>
            <ul>
                <li><strong>Ctrl + S</strong>: حفظ البيانات</li>
                <li><strong>F1</strong>: عرض هذه المساعدة</li>
                <li><strong>F5</strong>: تحديث الصفحة</li>
                <li><strong>Ctrl + P</strong>: طباعة التقرير</li>
            </ul>
            <p>للمزيد من المساعدة، تواصل مع الدعم الفني.</p>
        `;
        
        alert(helpContent);
    }
    
    // طرق مساعدة إضافية
    formatCurrency(amount, currency = 'SAR') {
        return new Intl.NumberFormat('ar-SA', {
            style: 'currency',
            currency: currency
        }).format(amount);
    }
    
    formatDate(date = new Date()) {
        return new Intl.DateTimeFormat('ar-SA', {
            dateStyle: 'full',
            timeStyle: 'medium'
        }).format(date);
    }
    
    validateEmail(email) {
        const regex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        return regex.test(email);
    }
    
    validatePhone(phone) {
        const regex = /^[\+]?[(]?[0-9]{3}[)]?[-\s\.]?[0-9]{3}[-\s\.]?[0-9]{4,6}$/;
        return regex.test(phone);
    }
}

// بدء التطبيق عند تحميل الصفحة
window.addEventListener('DOMContentLoaded', () => {
    window.app = new ElectricalAccountingApp();
    
    // إضافة زر مساعدة عائم
    const helpButton = document.createElement('button');
    helpButton.innerHTML = '<i class="fas fa-question-circle"></i>';
    helpButton.style.cssText = `
        position: fixed;
        bottom: 20px;
        left: 20px;
        background: #00ff88;
        color: #000428;
        border: none;
        width: 50px;
        height: 50px;
        border-radius: 50%;
        font-size: 1.5rem;
        cursor: pointer;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        z-index: 1000;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s;
    `;
    helpButton.addEventListener('mouseenter', () => {
        helpButton.style.transform = 'scale(1.1)';
    });
    helpButton.addEventListener('mouseleave', () => {
        helpButton.style.transform = 'scale(1)';
    });
    helpButton.addEventListener('click', () => {
        window.app.showHelp();
    });
    
    document.body.appendChild(helpButton);
});

// وظائف مساعدة عامة
function copyToClipboard(text) {
    navigator.clipboard.writeText(text)
        .then(() => console.log('📋 تم النسخ إلى الحافظة'))
        .catch(err => console.error('❌ فشل النسخ:', err));
}

function shareContent(title, text, url) {
    if (navigator.share) {
        navigator.share({ title, text, url })
            .then(() => console.log('✅ تم المشاركة بنجاح'))
            .catch(err => console.log('❌ فشل المشاركة:', err));
    }
}

// دعم وضع عدم الاتصال
if ('onLine' in navigator) {
    console.log('📡 حالة الاتصال:', navigator.onLine ? 'متصل' : 'غير متصل');
}

// تحسينات للأداء
if ('requestIdleCallback' in window) {
    window.requestIdleCallback(() => {
        console.log('⚡ استدعاء في وقت الفراغ');
    });
}

// تحميل الصور بذكاء
document.addEventListener('DOMContentLoaded', () => {
    const images = document.querySelectorAll('img[data-src]');
    const imageObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                const img = entry.target;
                img.src = img.dataset.src;
                imageObserver.unobserve(img);
            }
        });
    });
    
    images.forEach(img => imageObserver.observe(img));
});
EOF

        # 7. إنشاء ملف قراءة
        cat > README.md << 'EOF'
# ⚡ حسابات نهائية لأعمال الكهرباء

برنامج محاسبي متكامل لإدارة حسابات المقاولات الكهربائية، مبني باستخدام أحدث تقنيات الويب.

## ✨ المميزات

- ✅ إدارة مالية متكاملة
- ✅ نظام فواتير احترافي
- ✅ تقارير وإحصائيات متقدمة
- ✅ دعم كامل للغة العربية
- ✅ يعمل بدون اتصال بالإنترنت
- ✅ تطبيق ويب تقدمي (PWA)

## 🚀 النشر السريع

تم نشر التطبيق تلقائياً باستخدام GitHub Actions. كل تحديث على فرع `main` يؤدي إلى إعادة النشر تلقائياً.

## 🛠️ التقنيات المستخدمة

- HTML5, CSS3, JavaScript (ES6+)
- Service Workers ليعمل بدون اتصال
- Web App Manifest
- GitHub Actions للنشر المستمر
- تصميم متجاوب يعمل على جميع الأجهزة

## 📁 هيكل الملفات
